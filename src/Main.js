class HornedBeast {
  constructor (title, imageUrl, description) {
    this.title = title;
    this.imageUrl = imageUrl;
    this.description = description;
  }


} 

function Main(beasts) {
  return (
    <main>
      {
        beasts.forEach( el => {
          
        })
      }
    </main>
  );
}

export default Main;
